# MongoDB Atlas connection string (formato: mongodb+srv://user:pass@cluster.mongodb.net/?appName=...)
# Si falla ECONNREFUSED/querySrv: 1) Network Access → 0.0.0.0/0  2) Cluster activo  3) Internet
MONGODB_URI=your_connection_string

# Server port (default 4000)
PORT=4000

# JWT secret for auth (change in production)
JWT_SECRET=your_jwt_secret

# CORS: * allows all origins (e.g. http://localhost:5173). Or comma-separated list.
CORS_ORIGINS=*

# Set to "true" to enable JWT on /operations, /patrimony, /rates, /closing. Default: no auth.
# REQUIRE_AUTH=true
