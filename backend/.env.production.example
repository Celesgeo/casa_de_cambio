# Variables de entorno para producción (Render.com)
# Copia estos valores en Render Dashboard → Environment Variables

# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/?appName=exchange1

# Puerto (Render usa el puerto que proporciona en PORT, normalmente 10000)
PORT=10000

# JWT secret - CAMBIAR POR UNO SEGURO EN PRODUCCIÓN
JWT_SECRET=super_secret_jwt_key_change_me_in_production

# CORS: * permite todas las conexiones (útil para producción)
CORS_ORIGINS=*

# Requerir autenticación en producción
REQUIRE_AUTH=true

# Entorno
NODE_ENV=production
